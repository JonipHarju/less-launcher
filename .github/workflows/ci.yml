name: CI

on:
  push:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          # AGP 9.3 lint uses Java 21 collection APIs at runtime.
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: ./gradlew build

      - name: Run unit tests
        run: ./gradlew test

      - name: Run ktlint
        run: ./gradlew ktlintCheck
